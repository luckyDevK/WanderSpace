{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "lib": ["ES2020"],
    "emitDeclarationOnly": false,

    "typeRoots": ["./src/types"] // this is fine
  },
  "include": ["src", "src/types"], // make sure it's included
  "exclude": [] // don't exclude the types folder
}
